<!DOCTYPE html>
<html>
<head>
    <title>Indicative</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <link rel="stylesheet" href="/stylesheets/style.css" />
</head>
<body>
<div class="container">
    <div class="header clearfix">
        <nav>
            <ul class="nav nav-pills pull-right">
                {{#if user}}
                    <li role="presentation"><a href="/users/details">Kezdőoldal</a></li>
                    <li role="presentation"><a href="/users/logout">Kijelentkezés</a></li>
                {{else}}
                  <li role="presentation"><a href="/users/login">Adatok módosítása</a></li>
                  <li role="presentation"><a href="/users/login">Doksi</a></li>
                {{/if}}
            </ul>
        </nav>
      <a href="/"><h3 class="text-muted">Indicative</h3></a>
    </div>


    <div class="row">
        <div class="col-lg-12">
            {{#if success_msg}}
                <div class="alert alert-success">{{success_msg}}</div>
            {{/if}}

            {{#if error_msg}}
                <div class="alert alert-danger">{{error_msg}}</div>
            {{/if}}

            {{#if error}}
                <div class="alert alert-danger">{{error}}</div>
            {{/if}}

            {{{body}}}
        </div>


    </div>

    <footer class="footer" style="size: 7px; ">
        <!--<p >&copy; 2017 Magyar Balázs<br>magyarb94@gmail.com</p>-->
    </footer>

</div> <!-- /container -->
</body>
</html>
